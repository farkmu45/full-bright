server {
    listen 80;
    server_name _;
    root /var/www/html/public;
    index index.php;

    client_max_body_size 20m;

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;

    # Vite output is content-hashed, so it can be cached for a year
    location /build/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        # resolve "app" per request, so recreating the app container
        # does not leave nginx pointing at a stale IP (502)
        resolver 127.0.0.11 valid=10s ipv6=off;
        set $php_upstream app:9000;
        fastcgi_pass $php_upstream;
        # Laravel's preload Link header plus session cookies exceed the 4k
        # default, which makes nginx answer 502 "upstream sent too big header"
        fastcgi_buffer_size 32k;
        fastcgi_buffers 16 16k;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known) {
        deny all;
    }
}
